timezone: Europe/Rome
baseurl: /ui
permalink: pretty

# Future posts visibility
future: true

# COLLECTIONS
collections:
  docs:
    output: true
    title: Docs
  games:
    output: true
    title: Games

# DEFAULTS
defaults:
  - scope:
      path: ""
    values:
      layout: default
      sort_by: order